const encoding="UTF-8",mergeData=original=>new Promise(resolve=>{let chunks=[],size=0;original.on("data",chunk=>{chunks.push(chunk),size+=chunk.length}),original.on("end",()=>{let data=null,len=chunks.length;switch(len){case 0:data=new Buffer(0);break;case 1:data=chunks[0];break;default:data=new Buffer(size);for(let i=0,pos=0;i<len;i++){let chunk=chunks[i];chunk.copy(data,pos),pos+=chunk.length}}resolve(data)})}),isApi=(request,opt)=>{const path=request.path,type=opt.type,rules=opt.rules;"string"==typeof rules&&(rules=[rules]);const len=rules.length;let isApi=!1;if("prefix"===type){for(let i=0;i<len;i++)if(path.startsWith(rules[i])){isApi=!0;break}}else if("suffix"===type)for(let i=0;i<len;i++)if(path.endsWith(rules[i])){isApi=!0;break}return isApi},getParams=(request,method,isFormData)=>{if("POST"===method){let bodyData=request.body;return bodyData?Promise.resolve((isFormData?queryString.stringify:JSON.stringify)(bodyData)):mergeData(request)}if("GET"===method)return Promise.resolve(request.query)};module.exports={encoding:"UTF-8",mergeData:mergeData,isApi:isApi,getParams:getParams};