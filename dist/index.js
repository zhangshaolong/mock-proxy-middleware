"use strict";const URL=require("url"),utilsTool=require("./utils"),proxyTool=require("./proxy"),mockTool=require("./mock"),encoding=utilsTool.encoding;module.exports=(opts=>(req,res,next)=>{const urlInfo=URL.parse(req.url,!0);if(!utilsTool.isApi(urlInfo.pathname,opts))return next();{const isFormData=(req.headers["content-type"]||"text/plain;charset="+encoding).indexOf("application/x-www-form-urlencoded")>-1,method=req.method.toUpperCase(),headers={};for(let key in req.headers)headers[key]=req.headers[key];let proxyConfig=proxyTool.getProxy(req,opts.proxyConfig);utilsTool.getParams(req,urlInfo.query,method,isFormData,proxyConfig).then(params=>{proxyConfig?proxyTool.doProxy(req,res,headers,params,method,proxyConfig):mockTool.doMock(urlInfo.pathname,res,params,opts)})}});